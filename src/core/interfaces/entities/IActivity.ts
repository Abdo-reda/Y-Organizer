export interface IActivity {
    name: string;
    description: string;
    categories: string[]; //LifeCategoryEnum[]?
    color: string; //Theme? at leats two colors... make your own theme just chose two colors as well I guess... or one from a preset
}